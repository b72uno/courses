# Extra credit
# range(first, last) - generates a list including first and up to but not
# including the last digit
# yep elements += range(0,6) or elements.extend(range(0,6))
# ergh python doc on lists. len() .append() .extend() .insert .pop .index
# .count .sort .reverse. Cool
